language: c

compiler:
    - gcc
    - clang

script:
    - ./bootstrap
    - ./configure
    - make
    - ./programs/ekr_loop
    - ./configure --disable-crc32chw
    - make clean && make
    - ./programs/ekr_loop
    - ./configure --disable-shared --enable-static
    - make clean && make
    - ./programs/ekr_loop
